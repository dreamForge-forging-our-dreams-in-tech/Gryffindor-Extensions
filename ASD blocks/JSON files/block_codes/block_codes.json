{
    "evalCode": {
        "singleLine": true,
        "code": [
            "try {",
            "new Function('a', `return ",
            "[CODE]",
            "`.replaceAll(`'`, ``))();",
            "} catch (e) {",
            "console.error(`Error: ${e.message}`);",
            "}"
        ]
    },
    "evalCode_Reporter": {
        "singleLine": true,
        "code": [
            "() => {",
            "try {",
            "return new Function('a', `return ",
            "[CODE]",
            "`.replaceAll(`'`, ``))();",
            "} catch (e) {",
            "console.error(`Error: ${e.message}`);",
            "}",
            "}"
        ]
    }
}
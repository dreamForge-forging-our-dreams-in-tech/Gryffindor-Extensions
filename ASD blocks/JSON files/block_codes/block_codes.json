{
    "evalCode": {
        "singleLine": true,
        "code": [
            "try {",
            "new Function('a', `return ",
            "[CODE]",
            "`.replace(/[\r\n]+/g, ' '));",
            "} catch (e) {",
            "return `Error: ${e.message}`;",
            "}"
        ]
    },
    "evalCode_Reporter": {
        "singleLine": true,
        "code": [
            "() => {",
            "try {",
            "return new Function('a', `return ",
            "[CODE]",
            "`.replace(/[\r\n]+/g, ' '));",
            "} catch (e) {",
            "return `Error: ${e.message}`;",
            "}",
            "}"
        ]
    }
}
{
    "evalCode": {
        "singleLine": true,
        "code": [
            "try {",
            "const fn = new Function('a', `return ",
            "[CODE]",
            ");",
            "return fn(); // keep return statement because reporter reuses the same function",
            "} catch (e) {",
            "return `Error: ${e.message}`;",
            "}"
        ]
    },
    "evalCode_Reporter": {
        "singleLine": true,
        "code": [
            "() => {",
            "try {",
            "const fn = new Function('a', `return ",
            "[CODE]",
            ");",
            "return fn(); // keep return statement because reporter reuses the same function",
            "} catch (e) {",
            "return `Error: ${e.message}`;",
            "}",
            "}"
        ]
    }
}